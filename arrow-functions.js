// * Arrow Function basic syntax

// hello = () => {
//   return "Hello, world!";
// };

// * if one line statement then can use shorter this

// hello = () => "Hello, world!";

// * with parameters

//hello = (val) => "Hello, " + val;

// * if single  parameters

hello = (val) => "Hello, " + val;

console.log(hello("Shihab"));
